# 快速参考指南

## 🎯 快速开始

### 训练游戏

| 游戏 | 命令 | 预期时间 |
|------|------|---------|
| **Flappy Bird** | `train_flappybird.bat` 或<br>`python train.py --game flappybird --episodes 10000 --render-every 100` | ~2-4小时 |
| **CartPole** | `python train.py --game cartpole --episodes 500 --render-every 50` | ~10分钟 |
| **Snake** | `python train.py --game snake --episodes 1000 --render-every 100` | ~30分钟 |
| **LunarLander** | `python train.py --game lunarlander --episodes 2000 --render-every 100` | ~1小时 |

### 测试模型

| 游戏 | 命令 |
|------|------|
| **Flappy Bird** | `test_flappybird.bat` 或<br>`python test.py --game flappybird --model experiments/flappybird/models/best_model.pth --episodes 10 --render` |
| **CartPole** | `python test.py --game cartpole --model experiments/cartpole/models/best_model.pth --episodes 10 --render` |

## 📂 重要文件位置

### 训练输出
```
experiments/{game}/
├── models/
│   ├── best_model.pth           # ⭐ 最佳模型
│   ├── checkpoint_ep100.pth     # 定期检查点
│   └── final_model.pth          # 最终模型
└── logs/
    ├── training_history.pkl     # 训练历史（断点续训用）
    ├── training_episode_100.png # 📊 训练图表
    └── {game}_{N}ep.json        # JSON日志
```

### 配置文件
- `train.py` - 主训练脚本
- `test.py` - 主测试脚本
- `requirements.txt` - 依赖项
- `config.py` - 全局配置

## 🔧 常用参数

### 训练参数
```bash
python train.py \
  --game flappybird \           # 游戏名称
  --episodes 10000 \            # 训练轮数
  --render-every 100            # 每N轮渲染一次
```

### 测试参数
```bash
python test.py \
  --game flappybird \           # 游戏名称
  --model path/to/model.pth \   # 模型路径
  --episodes 10 \               # 测试轮数
  --render                      # 显示游戏画面
```

## 📊 训练监控

### 查看训练进度
1. 查看实时输出日志
2. 打开 `experiments/{game}/logs/training_episode_{N}.png`
3. 查看JSON日志: `experiments/{game}/logs/{game}_{N}ep.json`

### 训练图表内容
- **分数曲线**: 每回合分数 + 100回合移动平均
- **损失曲线**: 训练损失变化
- **Epsilon曲线**: 探索率衰减
- **统计信息**: 当前状态摘要

## ⚡ 性能提示

### 加速训练
1. 减少渲染频率: `--render-every 500`
2. 使用GPU（自动检测）
3. 增大batch size（在配置中）

### 节省空间
1. 删除旧检查点（保留最佳模型）
2. 定期清理日志图片
3. 只保留必要的模型

## 🐛 常见问题

### 训练太慢
- ✅ 减少 `--render-every` 值
- ✅ 检查是否使用GPU
- ✅ 减少训练轮数测试

### 模型效果不好
- ✅ 增加训练轮数
- ✅ 调整学习率
- ✅ 检查奖励函数设计

### 内存不足
- ✅ 减小batch size
- ✅ 减小memory buffer size
- ✅ 减少隐藏层大小

## 📋 检查清单

### 开始训练前
- [ ] 安装依赖: `pip install -r requirements.txt`
- [ ] 检查磁盘空间（至少1GB）
- [ ] （可选）确认GPU可用

### 训练过程中
- [ ] 监控训练日志输出
- [ ] 定期查看训练图表
- [ ] 检查模型是否保存

### 训练完成后
- [ ] 测试最佳模型
- [ ] 保存重要检查点
- [ ] 记录训练参数和结果

## 🎓 学习路径

### 新手推荐顺序
1. **CartPole** - 简单快速，理解基础
2. **Flappy Bird** - 完整示例，学习最佳实践  
3. **Snake** - 自定义环境，理解环境设计
4. **LunarLander** - 更复杂的任务

### 进阶探索
- 调整超参数
- 修改奖励函数
- 实现新的环境
- 尝试其他算法

## 📚 相关文档

- [完整README](README.md) - 项目概述
- [集成文档](FLAPPYBIRD_INTEGRATION.md) - 详细技术文档
- [集成总结](INTEGRATION_SUMMARY.md) - 完成情况总结
- [快速开始](QUICKSTART.md) - 详细入门指南

## 🆘 获取帮助

### 查看帮助
```bash
python train.py --help
python test.py --help
```

### 验证安装
```bash
python verify_integration.py
```

## 🎮 支持的游戏列表

| 游戏 | 算法 | 难度 | 状态空间 | 推荐轮数 |
|------|------|------|----------|----------|
| CartPole | DQN | ⭐ | 连续(4) | 500 |
| Flappy Bird | DQN | ⭐⭐ | 连续(7) | 10000 |
| Snake | DQN | ⭐⭐ | 离散 | 1000 |
| FrozenLake | Q-Learning | ⭐ | 离散(16) | 10000 |
| LunarLander | DQN | ⭐⭐⭐ | 连续(8) | 2000 |
| Pong | DQN | ⭐⭐⭐ | 图像 | 2000 |
| Breakout | DQN | ⭐⭐⭐ | 图像 | 5000 |

---

**快速上手，享受强化学习的乐趣！** 🚀
